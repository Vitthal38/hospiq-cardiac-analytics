name: hospiq
version: 1.0.0
config-version: 2
profile: hospiq

model-paths: ["models"]
test-paths: ["tests"]

models:
  hospiq:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
